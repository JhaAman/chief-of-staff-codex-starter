# Check-ins

Store only meaningful snapshots created from `templates/check-in.md`.

Use filenames such as `YYYY-MM-DD-HHMM.md`. Do not create a file when nothing
material changed, and do not copy raw source content into a check-in.
