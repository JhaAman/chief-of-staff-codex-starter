# Project notes

During onboarding, create a small Active Context from `templates/project.md`.
Keep useful supporting and historical notes discoverable through
`projects/index.md`; consult them when the user names work outside the default
context. Use short lowercase filenames such as `customer-onboarding.md`.
